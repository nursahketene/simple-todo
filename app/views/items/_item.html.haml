%li
  .line-content
    = link_to "Muista: #{item.title}", edit_item_path(item), :class => "item", :remote => true
    %span= link_to t('items.actions.done'), done_item_path(item), :class => "button" unless item.done
