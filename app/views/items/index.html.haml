
#new= link_to t('items.actions.new'), new_item_path, :remote => true

- if @todo.blank?
  %h3 Ei tekemättömiä tehtäviä
- else
  %ul#projects.todo
    = render :partial => 'item', :collection => @todo

%ul#projects.done
  - unless @done.blank?
    = render :partial => 'item', :collection => @done